import { makeStyles } from "@material-ui/core";

export default makeStyles({
  notAutorized: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    textAlign: "center",
  },
});
